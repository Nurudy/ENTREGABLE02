using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Chessboard
{

   /* private Vector2Int FichaChessPosition;
    private GameObject FichaGameObject;

    private int Ancho;
    private int Alto;

    public float Timer = 2f;

    public Chessboard(int w, int h)
    {
        Ancho = w;
        Alto = h;
    }


    void Update()
    {
        Timer -= Time.deltaTime;
        if (Timer <= 0f)
        {
            FichaChessPosition = (FichaChessPosition= new Vector2Int (Random.Range(-Ancho/ 2, Ancho / 2), Random.Range(-Alto/ 2, Alto / 2)));
            Timer = 2f;
        }
    }





    private void SpawnFicha()
    {
        FichaChessPosition = new Vector2Int(
                Random.Range(-Ancho / 2, Ancho / 2),
                Random.Range(-Alto / 2, Alto / 2));

        FichaGameObject = new GameObject("Ficha");
        SpriteRenderer fichaSpriteRenderer = FichaGameObject.AddComponent<SpriteRenderer>();
        fichaSpriteRenderer.sprite = GameChess.Instance.fileSprite;
        FichaGameObject.transform.position = new Vector3(FichaChessPosition.x, FichaChessPosition.y, 0);
    }*/










}
